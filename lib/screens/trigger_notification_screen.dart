import 'package:flutter/material.dart';

class TriggerNotificationScreen extends StatelessWidget {
  const TriggerNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}